services:
    app:
        container_name: bm-app
        build:
            context: .
            dockerfile: ./docker/Dockerfile
            target: production
            args:
                PHP_BUILD_IMAGE: ${PHP_BUILD_IMAGE:-mirror.gcr.io/library/php:8.4-cli}
                PHP_RUNTIME_IMAGE: ${PHP_RUNTIME_IMAGE:-mirror.gcr.io/library/php:8.4-apache}
        restart: unless-stopped
        volumes:
            - bm-app-storage:/var/www/storage
            - bm-app-assets:/var/www/public/build
        env_file:
            - .env
        environment:
            DB_CONNECTION: ${DB_CONNECTION:-mysql}
            # DB_HOST=127.0.0.1 in .env is for PHP on your machine; inside the container use the Compose network hostname.
            DB_HOST: ${DOCKER_DB_HOST:-mysql}
            DB_PORT: ${DB_PORT:-3306}
            DB_DATABASE: ${DB_DATABASE:-app}
            DB_USERNAME: ${DB_USERNAME:-laravel}
            DB_PASSWORD: ${DB_PASSWORD:-secret}
            DB_URL: ${DB_URL:-}
            REDIS_HOST: ${DOCKER_REDIS_HOST:-redis}
            REDIS_PORT: ${REDIS_PORT:-6379}
            REDIS_PASSWORD: ${REDIS_PASSWORD:-}
        networks:
            - bm-network
        ports:
            - "${APP_PORT:-8011}:80"
        healthcheck:
            test: ['CMD-SHELL', 'curl -fsS http://127.0.0.1/ >/dev/null || exit 1']
            interval: 10s
            timeout: 5s
            retries: 3
        depends_on:
            mysql:
                condition: service_healthy

    mysql:
        container_name: bm-db
        image: mysql:8.4
        restart: unless-stopped
        ports:
            - "${MYSQL_PORT:-8012}:3306"
        environment:
            MYSQL_DATABASE: ${DB_DATABASE:-app}
            MYSQL_USER: ${DB_USERNAME:-laravel}
            MYSQL_PASSWORD: ${DB_PASSWORD:-secret}
            MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-secret}
        volumes:
            - bm-db-store:/var/lib/mysql
        networks:
            - bm-network
        healthcheck:
            test: ['CMD', 'mysqladmin', 'ping', '-h', 'localhost']
            interval: 10s
            timeout: 5s
            retries: 5

    redis:
        container_name: bm-redis
        image: redis:alpine
        restart: unless-stopped
        networks:
            - bm-network
        healthcheck:
            test: ['CMD', 'redis-cli', 'ping']
            interval: 10s
            timeout: 5s
            retries: 3

networks:
    bm-network:

volumes:
    bm-db-store:
    bm-app-storage:
    bm-app-assets:
